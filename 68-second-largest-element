public Integer secondLargest(List<Integer> numbers) {
    //finding maximum number
    Set<Integer> numSet = new Set<Integer>();
    for(Integer num:numbers){
        numSet.add(num);
    }
    numSet.sort();
    return numSet.get(numSet.size()-2);

}